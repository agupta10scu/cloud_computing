1. docker run --name my_dba --network mynetwork -p8081:80 -d m
y_dba

2. docker run --name my_db --network mynetwork -itd -p5432:5432 my_db

3. docker run -it -p8000:8000 -v "$(pwd)/app:/app" my_django /bin/bash

4. python app/app/manage.py runserver 0.0.0.0:8000

5. docker run -it -p8000:8000 --network mynetwork -v "$(pwd)/app:/app" my_django /bin/bash

